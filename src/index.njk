---
layout: base.njk
title: "Wessley's - Terminal Portfolio"
description: "Software Developer Portfolio showcasing projects and skills in a terminal-themed interface"
keywords: "software developer, portfolio, programming, coding, terminal, developer"
ogTitle: "Wessley Nyakanyanga - Software Developer Portfolio"
ogDescription: "Terminal-themed portfolio showcasing projects and skills"
ogImage: "{{ '/' | url }}assets/img/profile.png"
ogUrl: "https://wessleyn.me"
canonical: "https://wessleyn.me"
showHireButton: true
---

{% block content %}
<h2 class="fs-3 fw-bold text-primary terminal-cursor">$ ls -la ~/projects</h2>
<p class="mb-4">I build innovative web applications and digital solutions that are
    responsive, accessible, and performant. Check out some of my recent projects below:</p>

<!-- Projects grid with improved horizontal centering and default image display -->
<div class="container-fluid">
    <div class="project-row" data-bss-type="blog-loop-base">
        {%- for project in collections.projects -%}
        <div data-bss-type="blog-loop-item">
            <div class="card border-0 project-item">
                <a class="stretched-link text-decoration-none"
                    href="{{ '/' | url }}project/{{ project.data.projectSlug }}"></a>
                <div class="d-flex justify-content-center align-items-center h-100 p-3">
                    <img class="card-img"
                        src="{{ '/' | url }}assets/img/projects/{{ project.data.projectImage }}"
                        alt="{{ project.data.projectName }}" />
                </div>

                {% if project.data.isPrivate %}
                <div class="project-private">
                    <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor"
                        class="bi bi-lock-fill" viewBox="0 0 16 16">
                        <path
                            d="M8 1a2 2 0 0 1 2 2v4H6V3a2 2 0 0 1 2-2zm3 6V3a3 3 0 0 0-6 0v4a2 2 0 0 0-2 2v5a2 2 0 0 0 2 2h6a2 2 0 0 0 2-2V9a2 2 0 0 0-2-2z" />
                    </svg>
                </div>
                {% endif %}

                {% if project.data.releaseDate %}
                <div class="project-countdown">
                    Coming {{ project.data.releaseDate }}
                </div>
                {% endif %}

                <div class="card-img-overlay text-white">
                    <h4 class="fs-6 fw-bold mb-1">{{ project.data.projectName }}</h4>
                    <p class="mb-0">{{ project.data.shortDescription }}</p>
                </div>
            </div>
        </div>
        {%- endfor %}
    </div>
</div>
{% endblock %}